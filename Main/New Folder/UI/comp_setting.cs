using UnityEngine;
using TMPro;

public class comp_setting : MonoBehaviour
{
    // [SerializeField] private main_settings main_settings;
    // [SerializeField] private Fpc Fpc;
    // [SerializeField] private TMP_Text txt;
    // [SerializeField] private string str;
    
    // private void Start() {
    //     main_settings = FindAnyObjectByType<main_settings>();
    //     Fpc = transform.parent.parent.parent.parent.parent.parent.parent.parent.parent.GetChild(0).GetComponent<Fpc>();
    //     settingsRend();
    // }
    // public void settingsAdd() {
    //     if (Fpc.p_rank == "Host") {
    //         for (int i = 0; i < main_settings.settings_com.Count; i++) {
    //             var dash = main_settings.settings_com[i];
    //             if (main_settings.settings_com[i].name == str) {
    //                 if (main_settings.settings_com[i].can_change) {
    //                     dash.active = !dash.active;
    //                     main_settings.settings_com[i] = dash;
    //                 }
    //             }
    //         }
    //     }
    // }
    // public void settingsRend() {
    //     for (int i = 0; i < main_settings.settings_com.Count; i++) {
    //         var dash = main_settings.settings_com[i];
    //         if (main_settings.settings_com[i].name == str) {
    //             if (main_settings.settings_com[i].active) txt.text = "On";
    //             else txt.text = "Off";
    //         }
    //     }
    // }
}
